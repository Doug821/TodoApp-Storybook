const {default: Button} = require('./components/Button/Button');
const {default: Input} = require('./components/Input/Input');
const {default: Card} = require('./components/Card/Card');
const {default: Home} = require('./Scenes/home/home');
const {default: Second} = require('./Scenes/second/second');

export {Button, Input, Card, Home, Second};
